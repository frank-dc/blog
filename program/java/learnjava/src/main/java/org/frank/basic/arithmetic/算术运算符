常见的运算符：
    算术运算符   +, -, *, /, %, ++, --
    赋值运算符   =
    比较（关系）运算符   >, <, ==, >=, <=
    逻辑运算符   ||，&&，!
    位运算符     &, |, ^, ~, >>, <<
    三目运算符   condition ? expression1 : expression2

在 Java 中
1. 使用算术运算符执行的操作总是产生一个int类型的结果。
2. 使用关系运算符执行的操作总是产生一个boolean类型的结果。