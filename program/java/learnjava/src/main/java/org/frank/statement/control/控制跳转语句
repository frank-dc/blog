`wc` 和 `nc` 在你的代码示例中是标签（label），用于标记循环语句。

在 Java 中，可以使用标签来标识循环语句，并在需要的时候使用 break 或 continue
语句来控制标签所在的循环。

在你的代码中，wc 是外部循环的标签，nc 是内部循环的标签。

这样做是为了在内部循环中使用 break 或 continue 语句时，能够指定是跳出哪个循环。

例如，如果你在内部循环中使用 break wc;，那么会跳出外部循环并继续执行外部循环之后的代码。